services:
  solarwatch:
    image: solarwatch
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:80"
    depends_on:
      - db
      - test_db
  db:
    image: mcr.microsoft.com/mssql/server:2019-latest
    environment:
      SA_PASSWORD: 121212EmQ1994?!
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - db_data:/var/opt/mssql/data
  test_db:
    image: mcr.microsoft.com/mssql/server:2019-latest
    environment:
      SA_PASSWORD: PaSsWoRd1Two3Four
      ACCEPT_EULA: "Y"
    ports:
        - "1434:1433"
    volumes:
      - test_db_data:/var/opt/mssql/data

volumes:
    db_data:
    test_db_data: